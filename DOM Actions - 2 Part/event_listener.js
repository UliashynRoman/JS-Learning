/* function select_func(event){
    let element = event;
    element = event.target;
    element = event.target.id;
    element = event.target.className;
    element = event.target.href;
    console.log(element);
}
 */
